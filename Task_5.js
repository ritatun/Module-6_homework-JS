const mulFunc = (x, n) => {
    if ((x > 0) && (n > 0) && (Number.isInteger(x)) && (Number.isInteger(n))) {
    const result = x **n;
    return result
    }
    return
    }
    
    let res = mulFunc(2, 2);
    console.log(res); 